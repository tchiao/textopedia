<div class="col-md-4">
  <% if text.image? %>
    <div><%= link_to image_tag(text.image.preview.url), text %></div>
  <% end %>
  
  <p>
    <div class="caption-title"><%= link_to text.title, text %></div>
    <p><small><%= text.authors %></small></p>
  </p>

</div>